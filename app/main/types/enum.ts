export enum Platform {
  MAC = 'darwin',
  WINDOW = 'win32',
  LINUX = 'linux',
}

export enum StoreKey {
  SWITCH = "SWITCH",
  LOGIN = "LOGIN",
  QUEUE = "QUEUE",
  WAIT_TIME = "WAIT_TIME",
  SERVICE = "SERVICE",
  CRONJOB = "CRONJOB",
}

export enum SwitchState {
  START = "START",
  PAUSED = "PAUSED",
  STOPPED = "STOPPED",
}
